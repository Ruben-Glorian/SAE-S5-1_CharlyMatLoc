<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8" />
    <title>Connecté</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
        body { font-family: Arial, sans-serif; background:#f7f7f7; color:#222; }
        .container { max-width: 800px; margin: 80px auto; padding: 24px; background: #fff; border-radius: 8px; box-shadow: 0 4px 14px rgba(0,0,0,0.06); }
        h1 { color: #2b6cb0; }
        .profile { margin-top: 12px; }
        .muted { color: #666; font-size: 0.95rem; }
    </style>
</head>
<body>
<div class="container">
    <h1>Vous êtes connecté</h1>

    {% if profile is defined and profile is not null %}
        <div class="profile">
            <p><strong>Identifiant :</strong> {{ profile.id }}</p>
            <p><strong>Email :</strong> {{ profile.email }}</p>
            {% if profile.role is defined %}
                <p><strong>Rôle :</strong> {{ profile.role }}</p>
            {% endif %}
        </div>
    {% else %}
        <p class="muted">Aucune information de profil fournie.</p>
    {% endif %}

    <button onclick="window.location.href='/catalogue'" style="margin-top:24px;padding:10px 16px;">Retour au catalogue</button>
</div>
</body>
</html>

